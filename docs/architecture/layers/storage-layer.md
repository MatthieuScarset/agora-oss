# Storage Layer

## Overview

The Storage Layer persists canonical entities in a queryable database (PostgreSQL with pgvector extension). Maintains lineage, audit trails, and supports point-in-time queries.

PostgreSQL, Redis, and object storage for persisting canonical data.
