# Retrieval Layer

## Overview

The Retrieval Layer provides efficient querying of canonical entities. Implements search, aggregation, filtering, and relationship traversal on top of the storage layer.

Hybrid search with BM25 and vector semantics.
