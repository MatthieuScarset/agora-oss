# Source Layer

## Overview

The Source Layer manages connections to external data sources (Drupal.org, WordPress.org, GitHub, etc.). Handles authentication, rate limiting, pagination, and incremental fetching.

External data sources that Agora ingests from.
