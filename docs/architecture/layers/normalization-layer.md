# Normalization Layer

## Overview

The Normalization Layer transforms source-native data into canonical entities. Implements the mapping rules DSL, identity stitching, deduplication, and enrichment logic.

Maps source-native data to canonical entity model.
