# Config Layer

## Overview

The Config Layer manages all external configuration for data sources, mappings, policies, and operational parameters. Configuration is stored in version-controlled YAML files and validated against JSON schemas at deployment time.

YAML/JSON configuration for sources, mappings, policies.
