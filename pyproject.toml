[project]
name = "agora-oss"
version = "0.1.0"
description = "Open-source knowledge graph for FLOSS community data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["mkdocs>=1.5.0", "mkdocs-material>=9.5.0"]
