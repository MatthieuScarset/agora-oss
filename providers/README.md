# Providers

This directory holds root-level provider modules discovered dynamically by Python.

Current setup:

- `providers/drupal.py` for the Drupal provider implementation
- provider defaults live in the provider module itself
